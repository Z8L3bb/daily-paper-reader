---
title: Learned Non-Maximum Suppression for 3D Object Detection
title_zh: 用于三维物体检测的学习型非极大抑制
authors: "Timo Osterburg, Stefan Schütte, Torsten Bertram"
date: 2026-06-02
pdf: "https://arxiv.org/pdf/2606.03568v1"
tags: ["query:stage-d-det"]
score: 9.0
evidence: 用于激光雷达三维目标检测的学习型非极大值抑制
tldr: 针对LiDAR 3D检测中启发式NMS导致密集提议筛选不理想的问题，提出两种学习的过滤模块D2D-Rescore和GossipNet3D，利用检测间关系替代传统NMS。结合nuScenes匹配策略，在nuScenes数据集上提升mAP、NDS及真阳性质量，尤其改善小目标与罕见类别，且计算开销小。证明检测级学习过滤可提升可靠性，无需修改骨干网络，为后处理提供原则性方案。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.03568v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1839, \"height\": 1050, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.03568v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 836, \"height\": 694, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.03568v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1501, \"height\": 264, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.03568v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1473, \"height\": 260, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.03568v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 667, \"height\": 378, \"label\": \"Table\"}]"
motivation: 传统NMS无法充分利用检测间关系，导致低质量过滤及小目标遗漏。
method: 提出D2D-Rescore基于Transformer注意力，GossipNet3D通过鸟瞰图局部消息传递实现3D检测过滤。
result: 相比CircleNMS，mAP、NDS和真阳性质量均提升，尤其小与罕见类别，计算开销小。
conclusion: 学习的检测过滤作为后处理插件，有效提升3D检测器可靠性，无需修改骨干，可替代启发式NMS。
---

## 摘要
后处理是基于激光雷达的三维物体检测的关键阶段，其中密集且相互重叠的候选框必须被过滤以实现紧凑可靠的感知。本文引入了两个学习型过滤模块，利用检测间的关系来替代启发式的非极大抑制（NMS）。D2D-Rescore采用基于Transformer的检测到检测（D2D）注意力机制，而GossipNet3D通过鸟瞰图中的局部消息传递将二维GossipNet概念适配到三维。与nuScenes评估协议对齐的度量感知匹配策略确保了训练和验证行为的一致性，从而提高了整体检测性能。与CircleNMS相比，两种方法均提升了平均精度均值（mAP）、nuScenes检测分数（NDS）和真阳性质量，特别是对于小样本和低频类别，并且仅增加了极小的计算开销。这些结果表明，学习型检测级过滤无需修改基础网络即可增强三维检测器的可靠性，为启发式抑制提供了一种有原则的替代方案。代码可在 https://github.com/rst-tu-dortmund/learned-3d-nms 获取。

## Abstract
Post-processing is a critical stage in LiDAR-based 3D object detection, where dense and overlapping proposals must be filtered for compact and reliable perception. This work introduces two learned filtering modules that replace heuristic non-maximum suppression (NMS) by leveraging relations among detections. D2D-Rescore employs transformer-based detection-to-detection (D2D) attention, while GossipNet3D adapts the 2D GossipNet concept to 3D through localized message passing in bird's-eye view. A metric-aware matching strategy aligned with the nuScenes evaluation protocol ensures consistent training and validation behavior, improving overall detection performance. Both approaches improve mean average precision (mAP), nuScenes detection score (NDS), and true positive quality compared to CircleNMS, particularly for small and infrequent classes, while adding minimal computational overhead. These results demonstrate that learned, detection-level filtering can enhance 3D detector reliability without modifying the base network, offering a principled alternative to heuristic suppression. Code is available at https://github.com/rst-tu-dortmund/learned-3d-nms .

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体意义

- **研究背景**：在基于激光雷达的三维目标检测中，密集且重叠的候选框（proposals）需要通过后处理过滤，以得到紧凑、可靠的感知结果。传统做法采用非极大抑制（NMS）与置信度阈值，这些启发式规则不可微、依赖人工调整的超参数，且忽略检测之间的上下文关系，容易在拥挤场景中误抑制有效目标或保留虚警。
- **核心问题**：如何利用检测间的相互关系，设计可学习的、轻量化的后处理模块，替代启发式NMS，从而提升三维检测的精度与鲁棒性，特别是对小目标和稀有类别。
- **整体意义**：本文证明了仅基于检测级特征的学习型滤波可以在不改变基础检测网络的前提下，有效增强三维检测器的可靠性，为后处理提供了一种有原则的、与检测器无关的替代方案。

### 2. 论文提出的方法论

- **核心思想**：设计两个可插入的后处理模块，接收检测器输出的密集候选集，通过聚合检测间上下文信息来调整每个检测的置信度分数，最后用 top‑K 选择代替传统 NMS。
- **两个模块**：
  - **D2D‑Rescore**（检测到检测重打分）：
    - 使用 Transformer 编码器建模检测间的全局关系。
    - 每个检测经 MLP 嵌入后，经过多层多头自注意力（Detection‑to‑Detection attention）与残差前馈网络，生成上下文增强的嵌入。
    - 引入可学习的温度参数以平衡注意力的集中与分散。
  - **GossipNet3D**（局部消息传递网络）：
    - 将二维 GossipNet 思想适配到三维，在鸟瞰图（BEV）中基于空间距离定义局部邻域（半径阈值 ε）。
    - 对每个检测与其邻居构建配对特征（包含几何关系如中心偏移、尺寸差、角度差、欧氏距离），通过 MLP 映射为消息，再对每个检测的邻居消息做最大池化，并与原始嵌入相加更新表征。
- **通用流程**：
  1. **输入嵌入**：检测框被编码为包含坐标（傅里叶特征）、尺寸、朝向（二维方向向量）、分类（独热编码）等的向量，再通过共享 MLP 映射到 p 维隐空间。
  2. **上下文聚合**：通过 D2D‑Rescore 或 GossipNet3D 将嵌入集合转换为上下文增强的嵌入集合 Z。
  3. **分数精调头**：用 MLP 从每个 zₑ 预测一个 logit 残差 ∆ℓ，以残差形式修正原始检测器输出的 logit ℓ：\(\hat{ℓ} = ℓ + Δℓ\)。
  4. **推理**：将修正后的 logit 转化为分数，取分数最高的 K 个检测作为最终输出，不执行任何 NMS 操作。
- **监督策略**：
  - 采用与 nuScenes 评估协议对齐的度量感知贪婪匹配策略，依次按中心点距离阈值（0.5 m, 1 m, 2 m, 4 m）为每个类别匹配真值，并进行一次类别无关的安全匹配，形成正负样本。
  - 使用二值交叉熵损失训练分数头，使修正后的分数符合匹配结果。

### 3. 实验设计

- **数据集**：nuScenes 数据集，使用官方验证集评估，遵循其评估协议。
- **基础检测器**：CenterPoint，提取其预 NMS 的密集候选（仅保留分数 > 0.1 的候选）供所有后处理方法使用，保证公平对比。
- **对比方法**：
  - CircleNMS（CenterPoint 默认的基于 BEV 中心点距离的 NMS）
  - Soft‑CircleNMS（BEV 下的 Soft‑NMS 变体）
  - 基于图卷积的去重方法（GCN）[10]
  - GossipNet3D（本文提出的局部图方法）
  - D2D‑Rescore（本文提出的 Transformer 方法）
- **评估指标**：mAP、NDS、各类的平均精度（AP）以及定位、尺度、朝向、速度的真阳性质量误差（TP metrics），以及最大召回和 PR 曲线。

### 4. 资源与算力

- **硬件配置**：单块 NVIDIA RTX 3090 GPU，搭配 AMD Ryzen 9 5900X CPU。
- **训练配置**：batch size 32；优化器 AdamW，最大学习率 5.5×10⁻⁴，权重衰减 1×10⁻²，余弦退火学习率调度，100 个 epochs。
- **推理资源**：文中给出了 GPU/CPU 上的平均推理延迟和 GPU 峰值内存占用，例如 D2D‑Rescore 在 GPU 上约 6.66 ms（约 150 Hz），CPU 上约 9.35 ms（约 107 Hz），GPU 内存占用约 51.57 MB。
- **训练时长**：未明确说明总训练时长，仅提及训练 100 个 epochs。

### 5. 实验数量与充分性

- **主要实验数量**：
  - 综合性能对比（表 I，含 5 种方法，4 组 TP 误差指标 + mAP、NDS）。
  - 各类别 AP 对比（表 II，10 个类别）。
  - PR 曲线（汽车类别，图 2）。
  - 注意力图可视化（图 1 中的示意）。
  - 消融实验（表 III）：匹配策略（Hungarian vs metric‑aware）、保留检测数量 K（300, 150, 100）对 mAP/NDS 的影响。
  - 推理速度与内存对比（表 I 中 T_CPU、T_GPU、M_GPU）。
- **充分性与客观性**：
  - 所有评估均在 nuScenes 验证集上进行，使用官方评估脚本，对比基线统一提取相同候选集，消除了检测器差异的影响。
  - 消融实验分析了关键设计选择（匹配策略、K 值），结论稳健。
  - 然而，实验仅基于 CenterPoint 一种检测器和 nuScenes 单一数据集，缺乏跨检测器（如 PointPillars、DSVT）和跨数据集（如 Waymo Open Dataset）的验证。部分对比方法（如 GCN）性能较差，可能与其在三维密集候选上的适配不足有关。

### 6. 论文的主要结论与发现

- D2D‑Rescore 和 GossipNet3D 均能超越 CircleNMS，在 mAP 和 NDS 上取得一致提升，同时改善定位、尺度、朝向、速度误差。
- 在稀有类别（施工车辆、摩托车、自行车）和小目标（交通锥）上提升尤为显著，表明学习型滤波能更好保留易被 NMS 抑制的难例。
- GossipNet3D 在 mAP 上最优，但计算开销更大；D2D‑Rescore 在精度‑效率之间取得更好平衡，尤其适合资源受限部署。
- 度量感知匹配策略优于传统的匈牙利匹配，使训练更稳定且性能更高。
- 精调后的分数排名质量高，允许将保留检测数量 K 降至 150 甚至 100 而 mAP 损失极小，有利于下游跟踪等任务。

### 7. 优点

- **方法优势**：
  - 完全基于检测级特征，不需修改探测器、不需访问骨干特征或原始点云，具有较强的通用性和即插即用特性。
  - 两种设计各有侧重：D2D‑Rescore 利用全局注意力，GossipNet3D 利用局部邻域，为不同部署场景提供选择。
  - 残差分数结构保留了检测器先验，利用上下文信息进行微调，训练稳定。
- **实验设计亮点**：
  - 在统一候选集上公平对比多种后处理方法。
  - 采用与评估协议对齐的匹配策略，减少训练‑测试不一致。
  - 给出详细的效率‑精度的权衡分析以及 TP 质量指标，兼顾召回数量和定位质量。
- **实用价值**：计算开销小，推理速度快，内存占用低，易于部署。

### 8. 不足与局限

- **检测器与数据集覆盖有限**：仅基于 CenterPoint 和 nuScenes 验证方法，未在其他主流激光雷达检测器（如 PointPillars、DSVT）及更大规模数据集（如 Waymo）上验证泛化性。
- **未处理全量框回归**：本文仅对分数进行修正，未涉及边界框位置的进一步精调，仍依赖原始检测器的定位质量。
- **局部模块优势未完全解释**：GossipNet3D 精度略高于 D2D‑Rescore，虽然通过注意力图给出了一些直观解释，但为何受限的局部聚合优于全局注意力仍缺乏深入理论或定量分析。
- **对比基线可能不完全**：Soft‑CircleNMS 表现不佳，但文中未深入探究其参数调优；GCN 方法失败可能与图构建方式有关，但未进一步优化或分析。
- **训练资源描述欠完整**：未报告总训练时长，难以直接比较训练成本。
- **偏差风险**：所有实验在同一检测器后处理管道上进行，若检测器本身对某些类别有系统性偏差，学习型滤波可能会继承或放大这种偏差，但论文未作分析。

（完）
